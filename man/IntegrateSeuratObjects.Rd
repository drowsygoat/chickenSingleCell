% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IntegrateSeuratObjects.R
\name{IntegrateSeuratObjects}
\alias{IntegrateSeuratObjects}
\title{Integrate Multiple Seurat Objects with Mitochondrial Regression}
\usage{
IntegrateSeuratObjects(
  seurat_list,
  min_features = 200,
  mito_threshold = 10,
  assay_type = "RNA",
  regress_mito = TRUE,
  regress_vars = c("nFeature_RNA"),
  nfeatures = 2000,
  dims = 1:30,
  anchor_reduction = "cca",
  return_unintegrated = FALSE,
  diet = TRUE,
  diet_args = list(assays = "RNA", counts = TRUE, data = FALSE, scale.data = FALSE),
  k.weight = 100,
  log_warnings = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{seurat_list}{A list of \code{Seurat} objects to integrate.}

\item{min_features}{Minimum number of detected features per cell (default = 200).}

\item{mito_threshold}{Threshold for annotating high mitochondrial cells (default = 10).}

\item{assay_type}{Assay name to use for normalization and regression (default = "RNA").}

\item{regress_mito}{Logical; whether to regress out mitochondrial percentage (default = TRUE).}

\item{regress_vars}{Character vector of metadata columns to regress out during scaling.}

\item{nfeatures}{Number of variable features to select (default = 3000).}

\item{dims}{Number of PCA dimensions to use for integration (default = 1:30).}

\item{anchor_reduction}{Reduction method for finding anchors ("cca" or "rpca").}

\item{return_unintegrated}{Logical; if TRUE, returns merged but unintegrated object (default = FALSE).}

\item{diet}{Logical; whether to slim Seurat objects using DietSeurat (default = TRUE).}

\item{diet_args}{A named list of arguments passed to \code{DietSeurat} if \code{diet = TRUE}.}

\item{k.weight}{Number of neighbors to consider when weighting anchors (default = 100).}

\item{log_warnings}{Logical; if TRUE, prints any warnings encountered (default = TRUE).}

\item{verbose}{Logical; whether to print progress messages (default = TRUE).}
}
\value{
An integrated \code{Seurat} object with PCA run on the integrated assay,
or a merged unintegrated object if \code{return_unintegrated = TRUE}.
}
\description{
A universal function to preprocess and integrate multiple Seurat objects,
optionally regressing out mitochondrial content instead of filtering it out.
Supports flexible quality control, normalization, and integration via CCA or RPCA.
}
